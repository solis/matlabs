function [] = t070203_comp

%%% Задача %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

    u    = @(x, t)  sin(x) .* cos(t);
    u_t  = @(x, t) -sin(x) .* sin(t);
    u_x  = @(x, t)  cos(x) .* cos(t);
    u_xx = @(x, t) -sin(x) .* cos(t);
    u_xt = @(x, t) -cos(x) .* sin(t);

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

    f    = @(x, t) u_t(x, t) - 9 * u_xx(x, t);
    mu   = @(t)    u_x (0, t);
    nu   = @(t)    u_x (1, t);
    mu_t = @(t)    u_xt(0, t);
    nu_t = @(t)    u_xt(1, t);
    phi  = @(x)    u   (x, t);

%%% Приводим к однородным граничным условиям %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

    w    = @(x, t) (x - x.^2 / 2) .* mu(t) + x.^2 / 2 * nu(t);
    w_t  = @(x, t) (x - x.^2 / 2) .* mu_t(t) + x.^2 / 2 * nu_t(t);
    w_xx = @(x, t) -mu(t) + nu(t);

    g    = @(x, t) f(x, t) + 9 * w_xx(x, t) - w_t(x, t);
    v    = @(x, t) u(x, t) - w(x, t);

%%% Решение задачи Штурма-Лиувилля %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

    lambda = @(k) (pi * k).^2;
    lambdasqrt = @(k) pi * k;
    Xkf = @(k, x) cos(lambdasqrt(k)*x);
    XK_NORM_SQ = @(k) 0.5;

%%% Создадим сетку %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

    T_MIN = 0;
    T_MAX = 1.5;

    X_MIN = 0;
    X_MAX = 1;

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

    K = 5;
    T = T_MIN:0.1:T_MAX;
    X = X_MIN:0.01:X_MAX;

%%% Препроцессинг %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

    TRAPZ_STEP = 0.0001;
    TRAPZ_GRID_X = X_MIN:TRAPZ_STEP:X_MAX;
    XK_TRAPZ = zeros(K, length(TRAPZ_GRID_X));
    VTZERO = v(TRAPZ_GRID_X, 0);

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

    UA = zeros(length(T), length(X));
    XK = zeros(K, length(X));
    TK = zeros(length(T), K);
    UN = zeros(length(T), length(X));

    ABS_ERR = zeros(length(T), length(X));
    REL_ERR = zeros(length(T), length(X));

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

    for k = 1:K
        XK_TRAPZ(k, :) = Xkf(k, TRAPZ_GRID_X);
        XK(k, :) = Xkf(k, X);
    end

%%% Решение задачи Коши %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

    xik = @(k) trapz(TRAPZ_GRID_X, VTZERO .* XK_TRAPZ(k, :)) / XK_NORM_SQ(k);

    function [ y ] = gk(k, t)
        y = zeros(length(t));
        for n = 1:length(t)
            y(n) = trapz(TRAPZ_GRID_X, g(TRAPZ_GRID_X, t(n)) .* XK_TRAPZ(k, :)) / XK_NORM_SQ(k);
        end
    end

    function [ y ] = Tkf(k, t)
        [odet Tk ] = ode45(@(u, v)  -9*lambda(k)*v(1) + gk(k, u), [ T_MIN T_MAX ], [ xik(k) ]);
        y = interp1(odet, Tk, t);
    end

%%% Считаем Tk и само решение %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

    for k = 1:K
        TK(:, k) = Tkf(k, T);
    end

    V = TK * XK;

    for i = 1:length(T)
        for j = 1:length(X)
            UN(i, j) = (w(X(j), T(i)) + V(i, j));
            UA(i, j) = u(X(j), T(i));

            ABS_ERR(i, j) = abs(UN(i, j) - UA(i, j));
            REL_ERR(i, j) = abs(ABS_ERR(i, j) / UA(i, j));
        end
    end

%%% Экспорт %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

    save('solution.mat', 'UA', 'UN', 'X', 'T', 'ABS_ERR', 'REL_ERR');
end
